<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}}</title>
    <meta name="description" content="{{description}}">
    
    <!-- Bootstrap 5 CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #f8f9fa;
        }
        
        .page-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 2rem 0;
            margin-bottom: 2rem;
        }
        
        .stat-card {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            text-align: center;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            border-left: 4px solid transparent;
            margin-bottom: 1.5rem;
        }
        
        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        .stat-card.primary { border-left-color: #667eea; }
        .stat-card.success { border-left-color: #28a745; }
        .stat-card.warning { border-left-color: #ffc107; }
        .stat-card.info { border-left-color: #17a2b8; }
        
        .stat-number {
            font-size: 2.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }
        
        .stat-label {
            color: #6c757d;
            font-size: 0.875rem;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }
        
        .profile-card {
            background: white;
            border-radius: 12px;
            padding: 1.5rem;
            box-shadow: 0 2px 8px rgba(0,0,0,0.08);
            transition: all 0.3s ease;
            margin-bottom: 1rem;
            border-left: 4px solid #667eea;
        }
        
        .profile-card:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 16px rgba(0,0,0,0.12);
        }
        
        .profile-avatar {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            object-fit: cover;
            border: 3px solid #667eea;
        }
        
        .view-count {
            background: #667eea;
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-weight: 600;
            display: inline-block;
        }
        
        .action-btn {
            border-radius: 8px;
            padding: 0.5rem 1rem;
            font-weight: 500;
            transition: all 0.2s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
        }
        
        .action-btn:hover {
            transform: translateY(-1px);
            box-shadow: 0 4px 8px rgba(102, 126, 234, 0.3);
            color: white;
        }
        
        .recent-view-item {
            padding: 1rem;
            border-bottom: 1px solid #e9ecef;
            transition: background-color 0.2s ease;
        }
        
        .recent-view-item:hover {
            background-color: #f8f9fa;
        }
        
        .recent-view-item:last-child {
            border-bottom: none;
        }
    </style>
</head>

<body>
    <!-- Navigation -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/business/dashboard">
                <i class="fas fa-briefcase me-2"></i>Business Portal
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/business/dashboard">
                            <i class="fas fa-tachometer-alt me-2"></i>Dashboard
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="/business/profile-views">
                            <i class="fas fa-eye me-2"></i>Profile Views
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/business/jobs">
                            <i class="fas fa-briefcase me-2"></i>Jobs
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/business/applications">
                            <i class="fas fa-users me-2"></i>Applications
                        </a>
                    </li>
                </ul>
                <ul class="navbar-nav">
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                            <i class="fas fa-user-circle me-2"></i>Account
                        </a>
                        <ul class="dropdown-menu">
                            <li><a class="dropdown-item" href="/business/profile">Profile</a></li>
                            <li><a class="dropdown-item" href="/business/settings">Settings</a></li>
                            <li><hr class="dropdown-divider"></li>
                            <li><a class="dropdown-item" href="/business/logout">Logout</a></li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- Page Header -->
    <div class="page-header">
        <div class="container">
            <div class="row align-items-center">
                <div class="col">
                    <h1 class="mb-0">
                        <i class="fas fa-eye me-3"></i>Profile Views Analytics
                    </h1>
                    <p class="mb-0 opacity-75">Track and analyze profile views and user engagement</p>
                </div>
            </div>
        </div>
    </div>

    <!-- Main Content -->
    <div class="container my-4">
        <!-- Statistics Overview -->
        <div class="row mb-4">
            <div class="col-md-3 mb-3">
                <div class="stat-card primary">
                    <i class="fas fa-users fa-2x text-primary mb-3"></i>
                    <div class="stat-number">{{stats.totalUniqueProfiles}}</div>
                    <div class="stat-label">Unique Profiles</div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="stat-card success">
                    <i class="fas fa-eye fa-2x text-success mb-3"></i>
                    <div class="stat-number">{{stats.totalViews}}</div>
                    <div class="stat-label">Total Views</div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="stat-card warning">
                    <i class="fas fa-chart-line fa-2x text-warning mb-3"></i>
                    <div class="stat-number">{{stats.averageViewsPerProfile}}</div>
                    <div class="stat-label">Avg Views/Profile</div>
                </div>
            </div>
            <div class="col-md-3 mb-3">
                <div class="stat-card info">
                    <i class="fas fa-trophy fa-2x text-info mb-3"></i>
                    <div class="stat-number">{{stats.mostViewedProfile.user.fullName}}</div>
                    <div class="stat-label">Most Viewed</div>
                </div>
            </div>
        </div>

        <div class="row">
            <!-- Profile Views List -->
            <div class="col-md-8 mb-4">
                <div class="card">
                    <div class="card-header bg-white">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-users text-primary me-2"></i>
                            Profile Views Detail
                        </h5>
                    </div>
                    <div class="card-body">
                        {{#each profileViews}}
                        <div class="profile-card">
                            <div class="row align-items-center">
                                <div class="col-auto">
                                    {{#if user.avatar}}
                                        <img src="/uploads/avatars/{{user.avatar}}" alt="{{user.fullName}}" class="profile-avatar">
                                    {{else}}
                                        <div class="profile-avatar bg-primary text-white d-flex align-items-center justify-content-center">
                                            <i class="fas fa-user fa-2x"></i>
                                        </div>
                                    {{/if}}
                                </div>
                                <div class="col">
                                    <h6 class="mb-1">{{user.fullName}}</h6>
                                    <p class="text-muted mb-2">
                                        <i class="fas fa-envelope me-1"></i>{{user.email}}
                                        {{#if user.phone}}
                                            <span class="ms-3"><i class="fas fa-phone me-1"></i>{{user.phone}}</span>
                                        {{/if}}
                                    </p>
                                    <div class="d-flex align-items-center gap-3">
                                        <span class="view-count">
                                            <i class="fas fa-eye me-1"></i>{{viewCount}} Views
                                        </span>
                                        <small class="text-muted">
                                            <i class="fas fa-calendar me-1"></i>Last viewed: {{lastViewed}}
                                        </small>
                                    </div>
                                </div>
                                <div class="col-auto">
                                    <a href="/business/user-profile/{{user._id}}" class="action-btn btn-primary">
                                        <i class="fas fa-user me-1"></i>View Profile
                                    </a>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                        
                        {{#unless profileViews}}
                        <div class="text-center py-5">
                            <i class="fas fa-eye fa-3x text-muted mb-3"></i>
                            <h5 class="text-muted">No Profile Views Yet</h5>
                            <p class="text-muted">When candidates apply to your jobs, their profile views will appear here.</p>
                        </div>
                        {{/unless}}
                    </div>
                </div>
            </div>

            <!-- Recent Views -->
            <div class="col-md-4 mb-4">
                <div class="card">
                    <div class="card-header bg-white">
                        <h5 class="card-title mb-0">
                            <i class="fas fa-clock text-primary me-2"></i>
                            Recent Activity
                        </h5>
                    </div>
                    <div class="card-body">
                        {{#each stats.recentViews}}
                        <div class="recent-view-item">
                            <div class="d-flex align-items-start">
                                <div class="flex-grow-1">
                                    <h6 class="mb-1">{{userName}}</h6>
                                    <p class="text-muted mb-0">
                                        <i class="fas fa-briefcase me-1"></i>{{jobTitle}}
                                    </p>
                                    <small class="text-muted">
                                        <i class="fas fa-calendar me-1"></i>{{viewedAt}}
                                    </small>
                                </div>
                            </div>
                        </div>
                        {{/each}}
                        
                        {{#unless stats.recentViews}}
                        <div class="text-center py-4">
                            <i class="fas fa-clock fa-2x text-muted mb-2"></i>
                            <p class="text-muted mb-0">No recent activity</p>
                        </div>
                        {{/unless}}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
