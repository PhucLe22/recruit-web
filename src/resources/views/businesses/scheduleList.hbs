<!DOCTYPE html>
<html lang="vi">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bảng Thông Tin Người Dùng</title>
  <style>
    /* Tab Navigation */
    .status-tabs {
      display: flex;
      gap: 10px;
      padding: 15px 20px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 10px;
      margin: 20px 0;
      flex-wrap: wrap;
    }
    
    .tab-btn {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: rgba(255, 255, 255, 0.7);
      padding: 10px 20px;
      border-radius: 20px;
      cursor: pointer;
      display: flex;
      align-items: center;
      gap: 8px;
      transition: all 0.3s ease;
    }
    
    .tab-btn i {
      font-size: 14px;
    }
    
    .tab-btn.active {
      background: #6366f1;
      color: white;
      border-color: #6366f1;
    }
    
    .tab-btn .badge {
      background: rgba(0, 0, 0, 0.2);
      color: white;
      padding: 2px 8px;
      border-radius: 10px;
      font-size: 12px;
    }
    
    .tab-pane {
      display: none;
      animation: fadeIn 0.3s ease;
    }
    
    .tab-pane.active {
      display: block;
    }
    
    /* Interview Cards */
    .interview-cards {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap: 20px;
      margin: 20px 0;
    }
    
    .interview-card {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 12px;
      overflow: hidden;
      transition: transform 0.3s ease, box-shadow 0.3s ease;
      border-left: 4px solid #6366f1;
    }
    
    .interview-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 10px 20px rgba(0, 0, 0, 0.2);
    }
    
    .interview-card.in-progress {
      border-left-color: #10b981;
    }
    
    .interview-card.completed {
      border-left-color: #6b7280;
      opacity: 0.8;
    }
    
    .card-header {
      padding: 20px;
      display: flex;
      align-items: center;
      gap: 15px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .applicant-avatar {
      width: 50px;
      height: 50px;
      border-radius: 50%;
      object-fit: cover;
    }
    
    .applicant-info h4 {
      margin: 0;
      color: white;
      font-size: 16px;
    }
    
    .job-title {
      font-size: 14px;
      color: #a5b4fc;
      margin-top: 4px;
    }
    
    .card-body {
      padding: 20px;
      text-align: center;
    }
    
    .interview-time {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      margin-bottom: 15px;
      color: #e5e7eb;
    }
    
    .interview-time i {
      font-size: 20px;
      color: #818cf8;
    }
    
    .time {
      font-weight: 500;
    }
    
    .time-until, .time-remaining {
      font-size: 12px;
      color: #a5b4fc;
    }
    
    .no-schedule {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      color: #9ca3af;
      margin-bottom: 15px;
    }
    
    .btn-join, .btn-schedule {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 8px 16px;
      border-radius: 20px;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      border: none;
      font-size: 14px;
    }
    
    .btn-join {
      background: #4f46e5;
      color: white;
    }
    
    .btn-join:hover {
      background: #4338ca;
      transform: translateY(-2px);
    }
    
    .btn-schedule {
      background: rgba(255, 255, 255, 0.1);
      color: white;
    }
    
    .btn-schedule:hover {
      background: rgba(255, 255, 255, 0.2);
    }
    
    .card-actions {
      display: flex;
      padding: 15px;
      border-top: 1px solid rgba(255, 255, 255, 0.1);
      gap: 10px;
    }
    
    .btn-view-cv, .btn-note {
      flex: 1;
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
      padding: 8px;
      border-radius: 6px;
      background: transparent;
      border: 1px solid rgba(255, 255, 255, 0.1);
      color: #a5b4fc;
      cursor: pointer;
      transition: all 0.2s ease;
    }
    
    .btn-view-cv:hover, .btn-note:hover {
      background: rgba(99, 102, 241, 0.1);
      border-color: rgba(99, 102, 241, 0.3);
    }
    
    .empty-state {
      text-align: center;
      padding: 40px 20px;
      color: #9ca3af;
    }
    
    .empty-state i {
      font-size: 40px;
      margin-bottom: 15px;
      opacity: 0.5;
    }
    
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
    }
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: linear-gradient(135deg, #0f0f23 0%, #1a1a3a 50%, #2d1b69 100%);
      min-height: 100vh;
      color: #ffffff;
      position: relative;
      overflow-x: hidden;
    }

    body::before {
      content: '';
      position: fixed;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: 
        radial-gradient(circle at 20% 80%, rgba(120, 119, 198, 0.3) 0%, transparent 50%),
        radial-gradient(circle at 80% 20%, rgba(255, 119, 198, 0.15) 0%, transparent 50%),
        radial-gradient(circle at 40% 40%, rgba(120, 219, 255, 0.1) 0%, transparent 50%);
      pointer-events: none;
      z-index: -1;
    }

    .container {
      max-width: 1400px;
      margin: 20px auto;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      box-shadow: 0 20px 60px rgba(0, 0, 0, 0.4);
      overflow: hidden;
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .back-btn {
      position: fixed;
      top: 30px;
      left: 30px;
      display: flex;
      align-items: center;
      gap: 10px;
      padding: 12px 20px;
      background: rgba(255, 255, 255, 0.1);
      backdrop-filter: blur(20px);
      color: white;
      text-decoration: none;
      border-radius: 25px;
      font-weight: 500;
      transition: all 0.3s ease;
      border: 1px solid rgba(255, 255, 255, 0.2);
      z-index: 100;
    }

    .back-btn:hover {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(99, 102, 241, 0.4);
      border-color: transparent;
    }

    .back-btn i {
      font-size: 16px;
    }

    .header {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white;
      padding: 40px;
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    .header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    }

    .header h1 {
      margin: 0;
      font-size: 2.5rem;
      font-weight: 700;
      text-shadow: 0 2px 10px rgba(0, 0, 0, 0.3);
      background: linear-gradient(135deg, #ffffff 0%, #a855f7 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }

    .table-container {
      padding: 20px;
      overflow-x: auto;
      display: flex;
      flex-direction: column;
      min-height: 60vh;
    }
    
    table {
      width: 100%;
      border-collapse: collapse;
      margin-bottom: 20px;
      flex-grow: 1;
    }

    th {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white;
      padding: 20px 15px;
      text-align: left;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      font-size: 0.9rem;
      position: relative;
    }

    th::after {
      content: '';
      position: absolute;
      bottom: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    }

    td {
      padding: 18px 15px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      transition: all 0.3s ease;
      color: #ffffff;
    }

    tr:nth-child(even) {
      background-color: rgba(255, 255, 255, 0.02);
    }

    tr:hover {
      background-color: rgba(255, 255, 255, 0.08);
      transform: scale(1.01);
      box-shadow: 0 8px 25px rgba(0, 0, 0, 0.2);
    }

    .link {
      color: #6366f1;
      text-decoration: none;
      font-weight: 500;
      transition: all 0.3s ease;
      padding: 8px 15px;
      border-radius: 10px;
      display: inline-block;
      cursor: pointer;
      border: 1px solid rgba(99, 102, 241, 0.3);
      background: rgba(99, 102, 241, 0.1);
    }

    .link:hover {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white;
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(99, 102, 241, 0.4);
      border-color: transparent;
    }

    .job-tag {
      background: rgba(99, 102, 241, 0.1);
      color: #8b5cf6;
      padding: 4px 12px;
      border-radius: 20px;
      font-size: 0.8rem;
      font-weight: 500;
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      max-width: 200px;
      display: inline-block;
    }
    
    .no-data {
      text-align: center;
      padding: 40px;
      color: #a1a1aa;
      grid-column: 1 / -1;
    }
    
    .no-data i {
      font-size: 2rem;
      margin-bottom: 10px;
      opacity: 0.7;
    }
    
    /* Pagination Styles */
    .pagination {
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 15px;
      margin-top: 20px;
      padding: 10px 0;
    }
    
    .page-link {
      display: flex;
      align-items: center;
      gap: 5px;
      padding: 8px 15px;
      background: rgba(255, 255, 255, 0.1);
      color: #e0e0e0;
      text-decoration: none;
      border-radius: 6px;
      transition: all 0.3s ease;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }
    
    .page-link:hover {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(99, 102, 241, 0.3);
      border-color: transparent;
    }
    
    .page-info {
      color: #a1a1aa;
      font-size: 0.9rem;
      padding: 0 15px;
    }
    
    /* Status select styles */
    .status-select {
      padding: 6px 10px;
      border-radius: 6px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      background: rgba(255, 255, 255, 0.05);
      color: #e0e0e0;
      cursor: pointer;
      transition: all 0.3s ease;
    }
    
    .status-select:hover {
      border-color: rgba(99, 102, 241, 0.5);
    }
    
    .status-select:focus {
      outline: none;
      border-color: #6366f1;
      box-shadow: 0 0 0 2px rgba(99, 102, 241, 0.2);
    }
    
    /* Job tag styles */
    .job-tag {
      background: rgba(99, 102, 241, 0.2);
      color: #8b5cf6;
      padding: 4px 10px;
      border-radius: 4px;
      font-size: 0.85rem;
      white-space: nowrap;
    }
    
    /* Link styles */
    .link, .meeting-link {
      color: #8b5cf6;
      text-decoration: none;
      transition: all 0.2s ease;
      display: inline-flex;
      align-items: center;
      gap: 5px;
    }
    
    .link:hover, .meeting-link:hover {
      color: #6366f1;
      text-decoration: underline;
    }
    
    .no-cv {
      color: #a1a1aa;
      font-style: italic;
    }
    
    .no-data p {
      margin: 0;
      font-size: 1rem;
    }
    
    .meeting-link {
      color: #10b981;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      gap: 6px;
      transition: color 0.2s;
    }
    
    .meeting-link:hover {
      color: #34d399;
    }
    
    .no-cv {
      color: #a1a1aa;
      font-style: italic;
    }

    .status-select {
      background: rgba(255, 255, 255, 0.05);
      color: #ffffff;
      border: 2px solid rgba(255, 255, 255, 0.1);
      border-radius: 10px;
      padding: 8px 12px;
      font-size: 0.9rem;
      font-weight: 500;
      cursor: pointer;
      transition: all 0.3s ease;
      min-width: 120px;
    }

    .status-select:focus {
      outline: none;
      border-color: #6366f1;
      background: rgba(255, 255, 255, 0.1);
      box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2);
    }

    .status-select option {
      background: #1a1a3a;
      color: #ffffff;
      padding: 8px;
    }

    .status-select.loading {
      opacity: 0.6;
      cursor: not-allowed;
    }

    .email {
      color: rgba(255, 255, 255, 0.8);
      font-family: 'Courier New', monospace;
      background: rgba(255, 255, 255, 0.05);
      padding: 6px 10px;
      border-radius: 8px;
      font-size: 0.9rem;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .username {
      font-weight: 600;
      color: #e0e0e0;
      font-size: 1.1rem;
    }

    .user-info {
      display: flex;
      align-items: center;
      gap: 12px;
      padding: 8px 0;
    }

    .user-avatar {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      object-fit: cover;
      border: 2px solid rgba(99, 102, 241, 0.3);
      transition: transform 0.2s ease, border-color 0.2s ease;
    }

    .user-avatar:hover {
      transform: scale(1.05);
      border-color: #6366f1;
    }

    .avatar-placeholder {
      width: 42px;
      height: 42px;
      border-radius: 50%;
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      color: white;
      font-weight: 600;
      text-transform: uppercase;
    }

    /* CV Popup Styles */
    .cv-popup {
      display: none;
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      height: 100%;
      background-color: rgba(0, 0, 0, 0.95);
      z-index: 1000;
      opacity: 0;
      transition: opacity 0.3s ease;
      backdrop-filter: blur(10px);
    }

    .cv-popup.show {
      display: flex;
      opacity: 1;
    }

    .cv-popup-content {
      position: relative;
      margin: auto;
      background: rgba(255, 255, 255, 0.05);
      backdrop-filter: blur(20px);
      border-radius: 20px;
      box-shadow: 0 25px 80px rgba(0, 0, 0, 0.6);
      max-width: 90%;
      max-height: 90%;
      overflow: hidden;
      transform: scale(0.7);
      transition: transform 0.3s ease;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-popup.show .cv-popup-content {
      transform: scale(1);
    }

    .cv-popup-header {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white;
      padding: 25px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      position: relative;
    }

    .cv-popup-header::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 1px;
      background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.3), transparent);
    }

    .cv-popup-header h3 {
      margin: 0;
      font-size: 1.5rem;
      font-weight: 600;
    }

    .close-btn {
      background: rgba(255, 255, 255, 0.1);
      border: 1px solid rgba(255, 255, 255, 0.2);
      color: white;
      font-size: 1.5rem;
      cursor: pointer;
      padding: 0;
      width: 45px;
      height: 45px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: all 0.3s ease;
    }

    .close-btn:hover {
      background: rgba(255, 255, 255, 0.2);
      transform: rotate(90deg);
    }

    .cv-popup-body {
      padding: 25px;
      text-align: center;
      max-height: 70vh;
      overflow-y: auto;
      background: #1a1a2e;
    }
    
    .btn-download-cv {
      display: inline-flex;
      align-items: center;
      gap: 8px;
      padding: 10px 20px;
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white;
      border: none;
      border-radius: 8px;
      font-size: 14px;
      font-weight: 500;
      cursor: pointer;
      text-decoration: none;
      margin-top: 20px;
      transition: all 0.3s ease;
    }
    
    .btn-download-cv:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 15px rgba(99, 102, 241, 0.4);
    }
    
    .cv-iframe-container {
      position: relative;
      width: 100%;
      height: 600px;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
      overflow: hidden;
      margin-bottom: 20px;
    }
    
    .cv-iframe-container iframe {
      width: 100%;
      height: 100%;
      border: none;
      background: white;
    }

    .cv-image {
      max-width: 100%;
      height: auto;
      border-radius: 15px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
      transition: transform 0.3s ease;
    }

    .cv-image:hover {
      transform: scale(1.02);
    }

    .cv-iframe {
      width: 100%;
      height: 70vh;
      border-radius: 15px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.4);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-container {
      max-width: 900px;
      margin: 0 auto;
      padding: 20px;
      color: #e0e0e0;
      background: rgba(30, 30, 50, 0.7);
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
    }

    .cv-header {
      text-align: center;
      margin-bottom: 30px;
      padding-bottom: 20px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-header h2 {
      color: #8b5cf6;
      margin-bottom: 5px;
    }

    .cv-section {
      margin-bottom: 30px;
    }

    .cv-section h3 {
      color: #8b5cf6;
      margin-bottom: 15px;
      padding-bottom: 8px;
      border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-item {
      margin-bottom: 20px;
      padding: 15px;
      background: rgba(255, 255, 255, 0.03);
      border-radius: 8px;
      transition: all 0.3s ease;
    }

    .cv-item:hover {
      background: rgba(255, 255, 255, 0.05);
      transform: translateY(-2px);
    }

    .cv-item h4 {
      color: #a78bfa;
      margin-bottom: 5px;
    }

    .date {
      color: #a1a1aa;
      font-size: 0.9em;
      margin-bottom: 8px;
    }

    .skills-container {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
      margin-top: 10px;
    }

    .skill-tag {
      background: rgba(139, 92, 246, 0.2);
      color: #c4b5fd;
      padding: 4px 12px;
      border-radius: 15px;
      font-size: 0.85em;
    }

    .cv-pdf-preview {
      width: 100%;
      height: 80vh;
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 8px;
      overflow: hidden;
    }

    .cv-error {
      text-align: center;
      padding: 30px;
      color: #f87171;
    }

    .cv-error i {
      font-size: 3em;
      margin-bottom: 15px;
      display: block;
    }

    .error-details {
      margin-top: 15px;
      font-size: 0.9em;
      color: #fca5a5;
    }

    .cv-text {
      background: rgba(255, 255, 255, 0.05);
      padding: 25px;
      border-radius: 15px;
      text-align: left;
      max-height: 60vh;
      overflow-y: auto;
      white-space: pre-wrap;
      font-family: 'Courier New', monospace;
      font-size: 0.9rem;
      line-height: 1.5;
      color: rgba(255, 255, 255, 0.9);
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .cv-download {
      padding: 40px;
      text-align: center;
    }

    .file-icon {
      font-size: 4rem;
      margin-bottom: 20px;
      color: #6366f1;
    }

    .cv-download h4 {
      color: #ffffff;
      margin: 10px 0;
      font-size: 1.2rem;
    }

    .cv-download p {
      color: rgba(255, 255, 255, 0.7);
      margin: 10px 0;
    }

    .download-btn {
      background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
      color: white !important;
      padding: 12px 24px;
      border-radius: 12px;
      text-decoration: none;
      font-weight: 600;
      display: inline-block;
      margin-top: 15px;
      transition: all 0.3s ease;
      border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .download-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 10px 30px rgba(99, 102, 241, 0.4);
    }

    .cv-error {
      color: #ef4444;
      font-size: 1.1rem;
      padding: 40px;
    }

    .cv-loading {
      text-align: center;
      padding: 3rem 2rem;
      color: #a1a1aa;
      max-width: 500px;
      margin: 0 auto;
    }
    
    .cv-loading .spinner {
      width: 40px;
      height: 40px;
      margin: 0 auto 1rem;
      border: 4px solid rgba(255, 255, 255, 0.1);
      border-radius: 50%;
      border-top-color: #8b5cf6;
      animation: spin 1s ease-in-out infinite;
    }
    
    @keyframes spin {
      to { transform: rotate(360deg); }
    }
    
    .cv-header h3 {
      color: #fff;
      margin-bottom: 0.5rem;
      font-size: 1.5rem;
    }
    
    .cv-header .job-title {
      color: #a78bfa;
      font-size: 1.1rem;
      margin-bottom: 0.5rem;
    }
    
    .cv-header .email {
      color: #a1a1aa;
      margin-bottom: 1.5rem;
    }
    
    .no-data i {
      font-size: 2rem;
      margin-bottom: 10px;
    }
  </style>
</head>

<body>
  <!-- Back Button -->
  <a href="/business/dashboard" class="back-btn">
    <i class="fas fa-arrow-left"></i>
    Quay lại Dashboard
  </a>

  <div class="container">
    <div class="header">
      <h1>Quản Lý Lịch Phỏng Vấn</h1>
      <p>Xem và quản lý các cuộc phỏng vấn theo trạng thái</p>
    </div>
    
    <!-- Status Tabs -->
    <div class="status-tabs">
      <button class="tab-btn active" data-tab="upcoming">
        <i class="fas fa-clock"></i> Sắp diễn ra
        <span class="badge">{{groupCounts.upcoming}}</span>
      </button>
      <button class="tab-btn" data-tab="inProgress">
        <i class="fas fa-video"></i> Đang phỏng vấn
        <span class="badge">{{groupCounts.inProgress}}</span>
      </button>
      <button class="tab-btn" data-tab="completed">
        <i class="fas fa-check-circle"></i> Đã hoàn thành
        <span class="badge">{{groupCounts.completed}}</span>
      </button>
      <button class="tab-btn" data-tab="noSchedule">
        <i class="fas fa-question-circle"></i> Chưa đặt lịch
        <span class="badge">{{groupCounts.noSchedule}}</span>
      </button>
    </div>
    
    <!-- Tab Content -->
    <div class="tab-content">
      <!-- Upcoming Interviews -->
      <div class="tab-pane active" id="upcoming">
        <h3><i class="fas fa-clock"></i> Sắp diễn ra ({{groupCounts.upcoming}})</h3>
        {{#if scheduleGroups.upcoming.length}}
          <div class="interview-cards">
            {{#each scheduleGroups.upcoming}}
              {{> interviewCard this}}
            {{/each}}
          </div>
        {{else}}
          <div class="empty-state">
            <i class="fas fa-calendar-check"></i>
            <p>Không có cuộc phỏng vấn nào sắp diễn ra</p>
          </div>
        {{/if}}
      </div>

      <!-- In Progress Interviews -->
      <div class="tab-pane" id="inProgress">
        <h3><i class="fas fa-video"></i> Đang phỏng vấn ({{groupCounts.inProgress}})</h3>
        {{#if scheduleGroups.inProgress.length}}
          <div class="interview-cards">
            {{#each scheduleGroups.inProgress}}
              {{> interviewCard this}}
            {{/each}}
          </div>
        {{else}}
          <div class="empty-state">
            <i class="fas fa-video-slash"></i>
            <p>Hiện không có cuộc phỏng vấn nào đang diễn ra</p>
          </div>
        {{/if}}
      </div>

      <!-- Completed Interviews -->
      <div class="tab-pane" id="completed">
        <h3><i class="fas fa-check-circle"></i> Đã hoàn thành ({{groupCounts.completed}})</h3>
        {{#if scheduleGroups.completed.length}}
          <div class="interview-cards">
            {{#each scheduleGroups.completed}}
              {{> interviewCard this}}
            {{/each}}
          </div>
        {{else}}
          <div class="empty-state">
            <i class="fas fa-clipboard-check"></i>
            <p>Chưa có cuộc phỏng vấn nào hoàn thành</p>
          </div>
        {{/if}}
      </div>

      <!-- No Schedule -->
      <div class="tab-pane" id="noSchedule">
        <h3><i class="fas fa-question-circle"></i> Chưa đặt lịch ({{groupCounts.noSchedule}})</h3>
        {{#if scheduleGroups.noSchedule.length}}
          <div class="interview-cards">
            {{#each scheduleGroups.noSchedule}}
              {{> interviewCard this}}
            {{/each}}
          </div>
        {{else}}
          <div class="empty-state">
            <i class="fas fa-calendar-plus"></i>
            <p>Tất cả ứng viên đã được đặt lịch phỏng vấn</p>
          </div>
        {{/if}}
      </div>
    </div>
    
    <!-- Interview Card Partial -->
    <script type="text/x-handlebars-template" id="interview-card-template">
      <div class="interview-card {{status}}" data-id="{{_id}}">
        <div class="card-header">
          <img src="{{defaultAvatar avatar}}" alt="{{name}}" class="applicant-avatar">
          <div class="applicant-info">
            <h4>{{name}}</h4>
            <p class="text-muted">{{email}}</p>
            <div class="job-title">{{jobName}}</div>
          </div>
        </div>
        <div class="card-body">
          {{#if interviewTime}}
            <div class="interview-time">
              <i class="far fa-calendar-alt"></i>
              <div>
                <div class="time">{{formattedInterviewTime}}</div>
                {{#if timeUntil}}
                  <div class="time-until">Còn {{timeUntil}} giờ nữa</div>
                {{/if}}
                {{#if minutesRemaining}}
                  <div class="time-remaining">Còn {{minutesRemaining}} phút</div>
                {{/if}}
              </div>
            </div>
            <a href="{{meetingLink}}" target="_blank" class="btn-join">
              <i class="fas fa-video"></i> Vào phỏng vấn
            </a>
          {{else}}
            <div class="no-schedule">
              <i class="far fa-calendar-times"></i>
              <span>Chưa đặt lịch phỏng vấn</span>
            </div>
            <button class="btn-schedule">
              <i class="far fa-calendar-plus"></i> Đặt lịch
            </button>
          {{/if}}
      
      {{#if pagination}}
      <div class="pagination">
        {{#if pagination.hasPreviousPage}}
          <a href="?page={{pagination.previousPage}}" class="page-link">
            <i class="fas fa-chevron-left"></i> Trước
          </a>
        {{/if}}
        
        <span class="page-info">
          Trang {{pagination.page}} / {{pagination.totalPages}}
        </span>
        
        {{#if pagination.hasNextPage}}
          <a href="?page={{pagination.nextPage}}" class="page-link">
            Tiếp theo <i class="fas fa-chevron-right"></i>
          </a>
        {{/if}}
      </div>
      {{/if}}
    </div>
  </div>

  <!-- CV Popup Modal -->
  <div id="cvPopup" class="cv-popup">
    <div class="cv-popup-content">
      <div class="cv-popup-header">
        <h3 id="cvTitle">CV - </h3>
        <button class="close-btn" onclick="closeCvPopup()">&times;</button>
      </div>
      <div class="cv-popup-body" id="cvBody">
        <div class="cv-loading">
          <div class="spinner"></div>
          <p>Đang tải CV...</p>
        </div>
      </div>
    </div>
  </div>
  <script>
    // Register Handlebars partial for interview cards
    if (typeof Handlebars !== 'undefined') {
      const source = document.getElementById('interview-card-template')?.innerHTML;
      if (source) {
        Handlebars.registerPartial('interviewCard', source);
      } else {
        console.error('Could not find interview card template');
      }
    }

    // Tab switching functionality
    document.addEventListener('DOMContentLoaded', function() {
      const tabBtns = document.querySelectorAll('.tab-btn');
      const tabPanes = document.querySelectorAll('.tab-pane');
      
      tabBtns.forEach(btn => {
        btn.addEventListener('click', function() {
          const tabId = this.getAttribute('data-tab');
          
          // Update active tab
          tabBtns.forEach(b => b.classList.remove('active'));
          this.classList.add('active');
          
          // Show corresponding tab content
          tabPanes.forEach(pane => pane.classList.remove('active'));
          document.getElementById(tabId).classList.add('active');
        });
      });
      
      // Initialize first tab as active
      if (tabBtns.length > 0) tabBtns[0].click();
      
      // Handlebars partial already registered at the top of the script
    });
    // CV Popup Functions
    async function openCvPopup(applicationId, email) {
      const popup = document.getElementById('cvPopup');
      const title = document.getElementById('cvTitle');
      const body = document.getElementById('cvBody');

      // Show loading state
      title.textContent = `Đang tải CV của ${email}...`;
      body.innerHTML = `
        <div class="cv-loading">
          <div class="spinner"></div>
          <p>Đang tải thông tin CV của ${username}...</p>
        </div>`;

      popup.style.display = 'flex';
      setTimeout(() => {
        popup.classList.add('show');
      }, 10);

      try {
        // Log the URL being requested
        const apiUrl = `/business/applicant/cv/${applicationId}`;
        console.log('Fetching CV for application:', applicationId, 'Email:', email);

        // Fetch CV data from API
        const response = await fetch(apiUrl, {
          method: 'GET',
          headers: {
            'Accept': 'application/json',
            'Content-Type': 'application/json'
          },
          credentials: 'same-origin'
        });

        const responseData = await response.json();
        console.log('CV API Response:', responseData);

        if (!response.ok) {
          throw new Error(`HTTP error! status: ${response.status}`);
        }

        if (!responseData.success) {
          throw new Error(responseData.error || 'Không thể tải thông tin CV');
        }

        // Update title with applicant's name
        title.textContent = `CV - ${responseData.applicant?.name || email}`;

        // Display CV based on type
        if (responseData.cvData && responseData.cvData.type === 'file') {
          // For file CVs (PDF)
          body.innerHTML = `
            <div class="cv-container">
              <div class="cv-header">
                <h3>${responseData.applicant?.name || email}</h3>
                ${responseData.applicant?.jobTitle ? `<p class="job-title">${responseData.applicant.jobTitle}</p>` : ''}
                ${responseData.applicant?.email ? `<p class="email">${responseData.applicant.email}</p>` : ''}
              </div>
              <div class="cv-pdf-preview">
                <iframe src="/pdfjs/web/viewer.html?file=/uploads/${encodeURIComponent(responseData.cvData.fileName)}" 
                        width="100%" 
                        height="600px" 
                        style="border: none; background: #fff;"
                        onerror="this.onerror=null;this.innerHTML='<div style=\'padding: 20px; color: white; text-align: center;\'><i class=\'fas fa-exclamation-triangle\'></i><p>Không thể tải file CV. Vui lòng tải xuống và xem trực tiếp.</p></div>';this.style.backgroundColor='#1e1e2d';">
                </iframe>
                <div style="text-align: center; margin-top: 10px;">
                  <a href="/uploads/${encodeURIComponent(responseData.cvData.fileName)}" class="btn btn-primary" download>
                    <i class="fas fa-download"></i> Tải xuống CV
                  </a>
                </div>
              </div>
            </div>`;
        } else if (responseData.cvData && responseData.cvData.type === 'data') {
          // For structured CV data
          const cv = responseData.cvData;
          body.innerHTML = `
            <div class="cv-container">
              <div class="cv-header">
                <h2>${cv.fullName || responseData.applicant.name}</h2>
                <p>${cv.headline || responseData.applicant.jobTitle}</p>
                <p>${cv.email || responseData.applicant.email}</p>
                ${cv.phone ? `<p>${cv.phone}</p>` : ''}
              </div>
              
              ${cv.summary ? `
                <div class="cv-section">
                  <h3>Giới thiệu</h3>
                  <p>${cv.summary}</p>
                </div>
              ` : ''}
              
              ${cv.experience && cv.experience.length > 0 ? `
                <div class="cv-section">
                  <h3>Kinh nghiệm làm việc</h3>
                  ${cv.experience.map(exp => `
                    <div class="cv-item">
                      <h4>${exp.position} - ${exp.company}</h4>
                      <p class="date">${exp.startDate} - ${exp.endDate || 'Hiện tại'}</p>
                      <p>${exp.description || ''}</p>
                    </div>
                  `).join('')}
                </div>
              ` : ''}
              
              ${cv.education && cv.education.length > 0 ? `
                <div class="cv-section">
                  <h3>Học vấn</h3>
                  ${cv.education.map(edu => `
                    <div class="cv-item">
                      <h4>${edu.degree} - ${edu.institution}</h4>
                      <p class="date">${edu.startDate} - ${edu.endDate || 'Hiện tại'}</p>
                      <p>${edu.description || ''}</p>
                    </div>
                  `).join('')}
                </div>
              ` : ''}
              
              ${cv.skills && cv.skills.length > 0 ? `
                <div class="cv-section">
                  <h3>Kỹ năng</h3>
                  <div class="skills-container">
                    ${cv.skills.map(skill => `
                      <span class="skill-tag">${skill.name} ${skill.level ? `(${skill.level})` : ''}</span>
                    `).join('')}
                  </div>
                </div>
              ` : ''}
            </div>
          `;
        } else {
          throw new Error('Định dạng CV không được hỗ trợ');
        }
      } catch (error) {
        console.error('Error loading CV:', error);
        body.innerHTML = `
          <div class="cv-error">
            <i class="fas fa-exclamation-circle"></i>
            <p>Không thể tải thông tin CV. Vui lòng thử lại sau.</p>
            <p class="error-details">${error.message}</p>
          </div>
        `;
      }

      // Prevent body scroll
      document.body.style.overflow = 'hidden';
    }

    function closeCvPopup() {
      const popup = document.getElementById('cvPopup');
      popup.classList.remove('show');

      setTimeout(() => {
        popup.style.display = 'none';
        document.body.style.overflow = 'auto';
      }, 300);
    }

    // Close popup when clicking outside
    document.getElementById('cvPopup').addEventListener('click', function (e) {
      if (e.target === this) {
        closeCvPopup();
      }
    });

    // Close popup with Escape key
    document.addEventListener('keydown', function (e) {
      if (e.key === 'Escape') {
        closeCvPopup();
      }
    });

    function addNewRow() {
      const table = document.getElementById('userTable').getElementsByTagName('tbody')[0];
      const newRow = table.insertRow();

      const username = prompt('Nhập username:') || 'user_' + Date.now();
      const email = prompt('Nhập email:') || 'user@example.com';
      const job = prompt('Nhập công việc:') || 'Developer';
      const cvImageUrl = prompt('Nhập URL ảnh CV:') || '/uploads/default-cv.jpg';
      const meetLink = prompt('Nhập link Google Meet:') || '#';

      newRow.innerHTML = `
        <td class="username">${username}</td>
        <td class="email">${email}</td>
        <td>
          <select class="status-select">
            <option value="pending" selected>⏳ Pending</option>
            <option value="approved">✅ Approved</option>
            <option value="rejected">❌ Rejected</option>
            <option value="scheduled">📅 Scheduled</option>
          </select>
        </td>
        <td><span class="job-tag">${job}</span></td>
        <td><a href="#" class="link view-cv" data-img="${cvImageUrl}" data-username="${username}">Xem CV</a></td>
        <td><a href="${meetLink}" class="link" target="_blank">Meeting Setup</a></td>
      `;

      // Add hover effect to new row
      newRow.addEventListener('mouseenter', function () {
        this.style.transform = 'scale(1.01)';
      });

      newRow.addEventListener('mouseleave', function () {
        this.style.transform = 'scale(1)';
      });

      // Add click handler for new CV link
      const newCvLink = newRow.querySelector('.view-cv');
      newCvLink.addEventListener('click', function (e) {
        e.preventDefault();
        const imgSrc = this.getAttribute('data-img');
        const username = this.getAttribute('data-username');
        openCvPopup(imgSrc, username);
      });
    }

    // Add smooth scrolling and animations
    // Handle status change
    document.querySelectorAll('.status-select').forEach(select => {
      select.addEventListener('change', async function() {
        const applicationId = this.getAttribute('data-id');
        const newStatus = this.value;
        
        try {
          const response = await fetch(`/business/${applicationId}/update`, {
            method: 'PUT',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ status: newStatus })
          });
          
          if (!response.ok) {
            throw new Error('Cập nhật trạng thái thất bại');
          }
          
          // Show success message
          const successMsg = document.createElement('div');
          successMsg.className = 'success-message';
          successMsg.textContent = 'Cập nhật trạng thái thành công';
          document.body.appendChild(successMsg);
          
          // Remove message after 3 seconds
          setTimeout(() => {
            successMsg.remove();
          }, 3000);
          
        } catch (error) {
          console.error('Error updating status:', error);
          alert('Có lỗi xảy ra khi cập nhật trạng thái: ' + error.message);
          // Revert the select value
          this.value = this.getAttribute('data-original-value');
        }
      });
    });
    
    // Add animation to table rows
    document.addEventListener('DOMContentLoaded', function () {
      const rows = document.querySelectorAll('tbody tr');
      rows.forEach((row, index) => {
        row.style.opacity = '0';
        row.style.transform = 'translateY(20px)';
        setTimeout(() => {
          row.style.transition = 'all 0.5s ease';
          row.style.opacity = '1';
            e.preventDefault();
            const imgSrc = this.getAttribute('data-img');
            const username = this.getAttribute('data-username');
            openCvPopup(imgSrc, username);
          });
        }
            alert('Đây là link demo. Vui lòng thay thế bằng link thực tế.');
          }
        });
      });

      document.querySelectorAll('.status-select').forEach(select => {
        select.setAttribute('data-old', select.value);

        select.addEventListener('change', function () {
          const jobAppliedId = this.dataset.id;
          const newStatus = this.value;
          const oldStatus = this.getAttribute('data-old');

          // Thêm hiệu ứng loading
          this.disabled = true;
          this.classList.add('loading');

          fetch(`/business/${jobAppliedId}/update`, {
            method: 'PUT',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ status: newStatus })
          })
            .then(res => {
              if (!res.ok) throw new Error('Lỗi cập nhật');
              return res.json();
            })
            .then(data => {
              if (data.success) {
                this.setAttribute('data-old', newStatus);

                // Hiển thị thông báo thành công với animation
                const successToast = document.createElement('div');
                successToast.style.cssText = `
                  position: fixed;
                  top: 20px;
                  right: 20px;
                  background: linear-gradient(135deg, #6366f1 0%, #8b5cf6 100%);
                  color: white;
                  padding: 15px 25px;
                  border-radius: 25px;
                  font-weight: 600;
                  z-index: 9999;
                  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
                  animation: slideInRight 0.5s ease;
                  border: 1px solid rgba(255, 255, 255, 0.1);
                `;
                successToast.innerHTML = '<i class="fas fa-check-circle me-2"></i>Cập nhật trạng thái thành công!';
                document.body.appendChild(successToast);

                setTimeout(() => {
                  successToast.style.animation = 'slideOutRight 0.5s ease';
                  setTimeout(() => successToast.remove(), 500);
                }, 3000);
              } else {
                this.value = oldStatus;
                alert('❌ Cập nhật thất bại!');
              }
            })
            .catch(err => {
              console.error(err);
              this.value = oldStatus;
              alert('❌ Lỗi kết nối!');
            })
            .finally(() => {
              this.disabled = false;
              this.classList.remove('loading');
            });
        });
      });
    });
  </script>
</body>

</html>